{
  "name": "your-app",
  "version": "1.0.0",
  "scripts": {
    "test": "mocha --reporter mocha-junit-reporter"
  },
  "devDependencies": {
    "mocha": "^10.0.0",
    "mocha-junit-reporter": "^2.0.0"
  }
}

