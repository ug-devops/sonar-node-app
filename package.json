{
  "name": "your-app",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:unit": "mocha",
    "test:coverage": "nyc npm test"
  },
  "devDependencies": {
    "mocha": "^10.7.3",
    "mocha-junit-reporter": "^2.2.1",
    "nyc": "^15.0.0"
  }
}
